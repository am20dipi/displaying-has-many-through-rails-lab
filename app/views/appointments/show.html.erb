
<%= @appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>
<!-- .strftime converts a datetime object to different string formats; this one loads the Month date, year and then the time hour:minutes -->
<%= link_to  @appointment.doctor_name, doctor_path(@appointment.doctor_id) %>
<%= link_to  @appointment.patient_name, patient_path(@appointment.patient_id) %>